# Copy to .env.development.local for local dev (gitignored).
# Used only when running: next dev
# Production uses .env.production and must NOT use this file.

NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
